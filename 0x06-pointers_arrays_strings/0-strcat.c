#include "main.h"
#include <string.h>
/**
 * *_strcat - concatenates two string
 * @dest:string
 * @src:string
 * Return: 0 or 1
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
