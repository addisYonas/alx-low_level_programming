#include "main.h"

int add(int a, int b)
{
    int nAdd = a + b;
    return nAdd;
}
int sub(int a, int b)
{
    int nsub = a -  b;
    return nsub;
}

int mul(int a, int b)
{
    int nmul = a * b;
    return nmul;
}
int div(int a, int b)
{
    int ndiv = a / b;
    return ndiv;
}
int mod(int a, int b)
{
    int nmod = a % b;
    return nmod;
}


